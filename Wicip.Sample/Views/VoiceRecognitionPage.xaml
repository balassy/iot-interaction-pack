<Page
    x:Class="Wicip.Sample.Views.VoiceRecognitionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Wicip.Sample.Views"
		xmlns:Behaviors="using:Template10.Behaviors"
		xmlns:Controls="using:Template10.Controls"
		xmlns:Interactivity="using:Microsoft.Xaml.Interactivity"	
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

	<Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition />
		</Grid.RowDefinitions>

		<Controls:PageHeader Frame="{x:Bind Frame, Mode=OneWay}" Content="Voice recognition">
			<!-- Hide the ellipsis menu on the right side of the PageHeader -->
			<Interactivity:Interaction.Behaviors>
				<Behaviors:EllipsisBehavior Visibility="Collapsed" />
			</Interactivity:Interaction.Behaviors>
		</Controls:PageHeader>


		<StackPanel Orientation="Vertical" Grid.Row="1" Margin="15, 15, 15, 15">
			<StackPanel Orientation="Horizontal">
				<Button x:Name="btnListen" Content="Listen" Margin="0, 0, 10, 0" Click="btnListen_Click" />
				<CheckBox x:Name="cboListenContinuously" Content="Listen continuously" />
			</StackPanel>
			
			<TextBlock x:Name="tblRecognizedText" Text="Waiting..." Margin="0, 10, 0, 0" />
		</StackPanel>		
	</Grid>
</Page>
